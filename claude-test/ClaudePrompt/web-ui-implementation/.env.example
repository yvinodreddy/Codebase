# Application
NEXT_PUBLIC_APP_URL=https://paragroupcli.netlify.app

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-secret-key-here-replace-with-real-secret

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Note: ANTHROPIC_API_KEY is NOT stored here
# Users provide their own API keys via the UI after login
