#!/usr/bin/env bash
#
# CPP - ClaudePrompt wrapper for ultrathink.py
#
# This is the ClaudePrompt version of ULTRATHINK, completely isolated from TestPrompt.
# It calls ultrathink.py with all the same arguments.
#
# Usage:
#   cpp "your prompt"
#   cpp "your prompt" --api
#   cpp --file prompt.txt
#   cpp --help
#   cpp "prompt" -v     (verbose mode)
#

# Get the directory where this script is actually located (resolve symlinks)
SOURCE="${BASH_SOURCE[0]}"
while [ -h "$SOURCE" ]; do
  SCRIPT_DIR="$(cd -P "$(dirname "$SOURCE")" && pwd)"
  SOURCE="$(readlink "$SOURCE")"
  [[ $SOURCE != /* ]] && SOURCE="$SCRIPT_DIR/$SOURCE"
done
SCRIPT_DIR="$(cd -P "$(dirname "$SOURCE")" && pwd)"

# Call ultrathink.py with all arguments
exec python3 "$SCRIPT_DIR/ultrathink.py" "$@"
