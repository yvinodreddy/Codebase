#!/usr/bin/env python3
"""
Demo: Print BOTH Keyword and Semantic Results for Comparison

CRITICAL REQUIREMENT (Effective 2025-11-27):
- BOTH results MUST be printed in output for comparison
- Complete details (content, scores, metadata)
- Side-by-side comparison
- MANDATORY for all production use

This allows users to:
- See exactly what each method returns
- Understand differences between keyword vs semantic
- Make informed decisions
- Validate both methods work correctly

Run: ./demo_print_both_results.py
"""
import sys
sys.path.insert(0, '/home/user01/claude-test/ClaudePrompt')

from database.dual_context_retriever import DualContextRetriever
import logging

# Configure logging
logging.basicConfig(
    level=logging.INFO,
    format='%(levelname)s - %(message)s'
)

def main():
    """
    Demonstrate printing BOTH results to output file.
    """
    print("=" * 80)
    print("🔥 DEMO: PRINT BOTH RESULTS FOR COMPARISON")
    print("=" * 80)
    print()
    print("CRITICAL REQUIREMENT:")
    print("  • BOTH keyword AND semantic results MUST be visible")
    print("  • Complete details for each result")
    print("  • Side-by-side comparison")
    print("  • MANDATORY for production use")
    print()

    # Initialize retriever
    retriever = DualContextRetriever()

    # Output file
    output_file = "/tmp/dual_search_results.txt"
    print(f"Results will be written to: {output_file}")
    print()

    # Clear output file
    with open(output_file, 'w') as f:
        f.write("DUAL SEARCH RESULTS - KEYWORD VS SEMANTIC COMPARISON\n")
        f.write("=" * 80 + "\n")
        f.write("Generated by: demo_print_both_results.py\n")
        f.write("=" * 80 + "\n\n")

    # Test queries
    queries = [
        "authentication implementation",
        "error handling patterns",
        "database connection pooling"
    ]

    for i, query in enumerate(queries, 1):
        print(f"[{i}/{len(queries)}] Processing query: '{query}'")

        # Print both results to file
        formatted_output = retriever.print_both_results(
            query=query,
            k=5,
            output_file=output_file
        )

        # Also display to console (abbreviated)
        print("\nResults preview (full details in file):")
        print("-" * 80)
        lines = formatted_output.split('\n')
        # Show first 30 lines
        for line in lines[:30]:
            print(line)
        if len(lines) > 30:
            print(f"\n... and {len(lines) - 30} more lines (see file for full output)")
        print("-" * 80)
        print()

    print("=" * 80)
    print("✅ COMPLETE - ALL RESULTS SAVED")
    print("=" * 80)
    print()
    print(f"📄 View complete results: cat {output_file}")
    print()
    print("The file contains:")
    print("  ✅ Keyword search results (complete details)")
    print("  ✅ Semantic search results (complete details)")
    print("  ✅ Side-by-side comparison")
    print("  ✅ Confidence scores")
    print("  ✅ Validation status")
    print("  ✅ Recommendation")
    print()
    print("🎯 This allows you to:")
    print("  • See exactly what each method returns")
    print("  • Understand differences between methods")
    print("  • Make informed decisions")
    print("  • Validate both methods work correctly")
    print()
    print("=" * 80)


if __name__ == "__main__":
    main()
