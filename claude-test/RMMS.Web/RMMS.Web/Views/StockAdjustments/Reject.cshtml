@model RMMS.Models.Inventory.StockAdjustment
@{
    ViewData["Title"] = "Reject Stock Adjustment";
}

<div class="container">
    <h2 class="text-danger">
        <i class="fas fa-times-circle"></i>
        @ViewData["Title"]
    </h2>
    <hr />

    <div class="alert alert-danger">
        <h5><i class="fas fa-exclamation-triangle"></i> Reject Adjustment</h5>
        <p>Please review the adjustment details below and provide a reason for rejection.</p>
        <p class="mb-0"><strong>Note:</strong> Rejecting this adjustment will NOT update the inventory ledger.</p>
    </div>

    <div class="card mb-3">
        <div class="card-header bg-primary text-white">
            <h5>Adjustment Details</h5>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-4">Adjustment Code</dt>
                <dd class="col-sm-8"><strong>@Model.AdjustmentCode</strong></dd>

                <dt class="col-sm-4">Adjustment Date</dt>
                <dd class="col-sm-8">@Model.AdjustmentDate.ToString("dd MMM yyyy hh:mm tt")</dd>

                <dt class="col-sm-4">Adjustment Type</dt>
                <dd class="col-sm-8">
                    <span class="badge bg-info">
                        <i class="fas @Model.AdjustmentIcon"></i> @Model.AdjustmentTypeDisplay
                    </span>
                </dd>

                <dt class="col-sm-4">Product</dt>
                <dd class="col-sm-8">
                    @Model.Product?.ProductName<br>
                    <small class="text-muted">@Model.Product?.ProductCode</small>
                </dd>

                <dt class="col-sm-4">Warehouse</dt>
                <dd class="col-sm-8">
                    @Model.Warehouse?.WarehouseName<br>
                    <small class="text-muted">@Model.Warehouse?.WarehouseCode</small>
                </dd>

                <dt class="col-sm-4">Reason</dt>
                <dd class="col-sm-8"><span class="badge bg-secondary">@Model.Reason</span></dd>

                <dt class="col-sm-4">Before Quantity</dt>
                <dd class="col-sm-8">@Model.BeforeQuantity.ToString("N2")</dd>

                <dt class="col-sm-4">Adjustment</dt>
                <dd class="col-sm-8">
                    <strong class="@(Model.AdjustmentType == "Increase" ? "text-success" : "text-danger")">
                        @(Model.AdjustmentType == "Increase" ? "+" : "-")@Model.Quantity.ToString("N2")
                    </strong>
                </dd>

                <dt class="col-sm-4">After Quantity</dt>
                <dd class="col-sm-8">@Model.AfterQuantity.ToString("N2")</dd>

                @if (!string.IsNullOrEmpty(Model.Remarks))
                {
                    <dt class="col-sm-4">Remarks</dt>
                    <dd class="col-sm-8">@Model.Remarks</dd>
                }

                <dt class="col-sm-4">Created By</dt>
                <dd class="col-sm-8">@Model.CreatedBy</dd>

                <dt class="col-sm-4">Created Date</dt>
                <dd class="col-sm-8">@Model.CreatedDate.ToString("dd MMM yyyy hh:mm tt")</dd>
            </dl>
        </div>
    </div>

    <form asp-action="Reject" method="post">
        <input type="hidden" name="id" value="@Model.Id" />

        <div class="card mb-3">
            <div class="card-header bg-danger text-white">
                <h5>Rejection Reason (Required)</h5>
            </div>
            <div class="card-body">
                <textarea name="reason" class="form-control" rows="3" placeholder="Enter reason for rejection..." required></textarea>
                <div class="invalid-feedback">
                    Please provide a reason for rejection.
                </div>
            </div>
        </div>

        <div class="d-flex justify-content-between">
            <a asp-action="Index" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to List
            </a>
            <button type="submit" class="btn btn-danger">
                <i class="fas fa-times"></i> Confirm Rejection
            </button>
        </div>
    </form>
</div>
