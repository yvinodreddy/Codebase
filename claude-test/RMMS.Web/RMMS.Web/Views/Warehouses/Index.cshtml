@model RMMS.Common.Pagination.PagedResult<RMMS.Models.Inventory.Warehouse>
@{
    ViewData["Title"] = "Warehouses";
    string GetSortIcon(string columnName)
    {
        if (ViewBag.CurrentSort != columnName)
            return "<i class='fas fa-sort text-muted'></i>";

        return ViewBag.CurrentOrder == "asc"
            ? "<i class='fas fa-sort-up'></i>"
            : "<i class='fas fa-sort-down'></i>";
    }

    string GetSortUrl(string columnName)
    {
        var order = ViewBag.CurrentSort == columnName && ViewBag.CurrentOrder == "asc" ? "desc" : "asc";
        return $"?searchTerm={ViewBag.SearchTerm}&sortBy={columnName}&sortOrder={order}&page=1";
    }
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-warehouse"></i> Warehouse Management</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> New Warehouse
        </a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="card">
        <div class="card-header bg-primary text-white">
            <div class="row">
                <div class="col-md-6">
                    <h5 class="mb-0"><i class="fas fa-list"></i> Warehouse List</h5>
                </div>
                <div class="col-md-6 text-end">
                    <span class="badge bg-light text-dark">Total: @Model.TotalItems</span>
                </div>
            </div>
        </div>
        <div class="card-body">
            <!-- Search Form -->
            <form method="get" class="row g-3 mb-3">
                <div class="col-md-10">
                    <input type="text" name="searchTerm" value="@ViewBag.SearchTerm"
                           class="form-control" placeholder="Search by warehouse name, code, or location...">
                    <input type="hidden" name="sortBy" value="@ViewBag.CurrentSort" />
                    <input type="hidden" name="sortOrder" value="@ViewBag.CurrentOrder" />
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="fas fa-search"></i> Search
                    </button>
                </div>
            </form>

            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>
                                <a href="@GetSortUrl("WarehouseCode")" class="text-white text-decoration-none">
                                    Code @Html.Raw(GetSortIcon("WarehouseCode"))
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("WarehouseName")" class="text-white text-decoration-none">
                                    Name @Html.Raw(GetSortIcon("WarehouseName"))
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("City")" class="text-white text-decoration-none">
                                    Location @Html.Raw(GetSortIcon("City"))
                                </a>
                            </th>
                            <th>Type</th>
                            <th>
                                <a href="@GetSortUrl("TotalCapacity")" class="text-white text-decoration-none">
                                    Capacity (MT) @Html.Raw(GetSortIcon("TotalCapacity"))
                                </a>
                            </th>
                            <th>Used (MT)</th>
                            <th>Available (MT)</th>
                            <th>
                                <a href="@GetSortUrl("Status")" class="text-white text-decoration-none">
                                    Status @Html.Raw(GetSortIcon("Status"))
                                </a>
                            </th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Items.Any())
                        {
                            @foreach (var warehouse in Model.Items)
                            {
                                <tr>
                                    <td><strong>@warehouse.WarehouseCode</strong></td>
                                    <td>@warehouse.WarehouseName</td>
                                    <td>@warehouse.City, @warehouse.State</td>
                                    <td>
                                        <span class="badge bg-info">@warehouse.WarehouseType</span>
                                    </td>
                                    <td>@warehouse.TotalCapacity.ToString("N2")</td>
                                    <td>@warehouse.UsedCapacity.ToString("N2")</td>
                                    <td>@warehouse.AvailableCapacity.ToString("N2")</td>
                                    <td>
                                        <span class="badge bg-@(warehouse.Status == "Active" ? "success" : warehouse.Status == "Full" ? "danger" : warehouse.Status == "Maintenance" ? "warning" : "secondary")">
                                            @warehouse.Status
                                        </span>
                                    </td>
                                    <td>
                                        <div class="btn-group btn-group-sm" role="group">
                                            <a asp-action="Details" asp-route-id="@warehouse.Id" class="btn btn-info" title="Details">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            <a asp-action="Edit" asp-route-id="@warehouse.Id" class="btn btn-warning" title="Edit">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@warehouse.Id" class="btn btn-danger" title="Delete">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="9" class="text-center py-4">
                                    <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                                    <p class="text-muted">No warehouses found</p>
                                    @if (!string.IsNullOrEmpty(ViewBag.SearchTerm))
                                    {
                                        <a href="@Url.Action("Index")" class="btn btn-sm btn-outline-primary">Clear Search</a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <strong>Showing @Model.Items.Count of @Model.TotalItems warehouses</strong>
                </div>
                <div class="col-md-6">
                    @await Html.PartialAsync("_PaginationPartial", Model)
                </div>
            </div>
        </div>
    </div>
</div>

