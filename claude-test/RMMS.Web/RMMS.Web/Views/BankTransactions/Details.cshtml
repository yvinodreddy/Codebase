@model RMMS.Models.BankTransaction
@{
    ViewData["Title"] = "Bank Transaction Details";
}

<div class="container-fluid">
    <div class="card shadow">
        <div class="card-header bg-info text-white">
            <h3><i class="fas fa-university"></i> Bank Transaction Details</h3>
        </div>

        <div class="card-body">
            <div class="row">
                <div class="col-md-6">
                    <dl class="row">
                        <dt class="col-sm-5">Transaction Date:</dt>
                        <dd class="col-sm-7">@Model.TransactionDate.ToString("dd-MMM-yyyy")</dd>

                        <dt class="col-sm-5">Cheque/UTR No:</dt>
                        <dd class="col-sm-7">@(Model.ChequeUtrNumber ?? "N/A")</dd>

                        <dt class="col-sm-5">Bank Name:</dt>
                        <dd class="col-sm-7">@Model.BankName</dd>

                        <dt class="col-sm-5">Account Number:</dt>
                        <dd class="col-sm-7">@Model.AccountNumber</dd>

                        <dt class="col-sm-5">Particulars:</dt>
                        <dd class="col-sm-7">@Model.Particulars</dd>
                    </dl>
                </div>

                <div class="col-md-6">
                    <dl class="row">
                        <dt class="col-sm-5">Deposits:</dt>
                        <dd class="col-sm-7 text-success fw-bold">
                            @(Model.Deposits > 0 ? "₹ " + Model.Deposits.ToString("N2") : "-")
                        </dd>

                        <dt class="col-sm-5">Withdrawals:</dt>
                        <dd class="col-sm-7 text-danger fw-bold">
                            @(Model.Withdrawals > 0 ? "₹ " + Model.Withdrawals.ToString("N2") : "-")
                        </dd>

                        <dt class="col-sm-5">Balance:</dt>
                        <dd class="col-sm-7 fw-bold">₹ @Model.Balance.ToString("N2")</dd>

                        <dt class="col-sm-5">Remarks:</dt>
                        <dd class="col-sm-7">@(Model.Remarks ?? "N/A")</dd>

                        <dt class="col-sm-5">Created Date:</dt>
                        <dd class="col-sm-7">@Model.CreatedDate.ToString("dd-MMM-yyyy HH:mm")</dd>
                    </dl>
                </div>
            </div>

            <hr />

            <div class="mt-4">
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">
                    <i class="fas fa-edit"></i> Edit
                </a>
                <form asp-action="Delete" asp-route-id="@Model.Id" method="post" style="display: inline;"
                      onsubmit="return confirm('Are you sure you want to delete this transaction?');">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash"></i> Delete
                    </button>
                </form>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Back to List
                </a>
            </div>
        </div>
    </div>
</div>
