@model RMMS.Common.Pagination.PagedResult<RMMS.Models.Masters.Customer>
@{
    ViewData["Title"] = "Customers";
    string GetSortIcon(string columnName)
    {
        if (ViewBag.CurrentSort != columnName)
            return "<i class='fas fa-sort text-muted'></i>";

        return ViewBag.CurrentOrder == "asc"
            ? "<i class='fas fa-sort-up'></i>"
            : "<i class='fas fa-sort-down'></i>";
    }

    string GetSortUrl(string columnName)
    {
        var order = ViewBag.CurrentSort == columnName && ViewBag.CurrentOrder == "asc" ? "desc" : "asc";
        return $"?searchTerm={ViewBag.SearchTerm}&sortBy={columnName}&sortOrder={order}&page=1";
    }
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-users"></i> Customer Management</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> New Customer
        </a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="card">
        <div class="card-header bg-primary text-white">
            <div class="row">
                <div class="col-md-6">
                    <h5 class="mb-0"><i class="fas fa-list"></i> Customer List</h5>
                </div>
                <div class="col-md-6 text-end">
                    <span class="badge bg-light text-dark">Total: @Model.TotalItems</span>
                </div>
            </div>
        </div>
        <div class="card-body">
            <!-- Search Form -->
            <form method="get" class="row g-3 mb-3">
                <div class="col-md-10">
                    <input type="text" name="searchTerm" value="@ViewBag.SearchTerm"
                           class="form-control" placeholder="Search by name, code, or GSTIN...">
                    <input type="hidden" name="sortBy" value="@ViewBag.CurrentSort" />
                    <input type="hidden" name="sortOrder" value="@ViewBag.CurrentOrder" />
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="fas fa-search"></i> Search
                    </button>
                </div>
            </form>

            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>
                                <a href="@GetSortUrl("CustomerCode")" class="text-white text-decoration-none">
                                    Code @Html.Raw(GetSortIcon("CustomerCode"))
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("CustomerName")" class="text-white text-decoration-none">
                                    Name @Html.Raw(GetSortIcon("CustomerName"))
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("CustomerType")" class="text-white text-decoration-none">
                                    Type @Html.Raw(GetSortIcon("CustomerType"))
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("GSTIN")" class="text-white text-decoration-none">
                                    GSTIN @Html.Raw(GetSortIcon("GSTIN"))
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("CreditLimit")" class="text-white text-decoration-none">
                                    Credit Limit @Html.Raw(GetSortIcon("CreditLimit"))
                                </a>
                            </th>
                            <th>
                                <a href="@GetSortUrl("Status")" class="text-white text-decoration-none">
                                    Status @Html.Raw(GetSortIcon("Status"))
                                </a>
                            </th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Items.Any())
                        {
                            @foreach (var customer in Model.Items)
                            {
                                <tr>
                                    <td><strong>@customer.CustomerCode</strong></td>
                                    <td>@customer.CustomerName</td>
                                    <td>
                                        <span class="badge bg-info">@customer.CustomerType</span>
                                    </td>
                                    <td>@customer.GSTIN</td>
                                    <td>₹ @customer.CreditLimit?.ToString("N2")</td>
                                    <td>
                                        <span class="badge bg-@(customer.Status == "Active" ? "success" : "secondary")">
                                            @customer.Status
                                        </span>
                                    </td>
                                    <td>
                                        <div class="btn-group btn-group-sm" role="group">
                                            <a asp-action="Details" asp-route-id="@customer.Id" class="btn btn-info" title="Details">
                                                <i class="fas fa-eye"></i>
                                            </a>
                                            <a asp-action="Edit" asp-route-id="@customer.Id" class="btn btn-warning" title="Edit">
                                                <i class="fas fa-edit"></i>
                                            </a>
                                            <a asp-action="Delete" asp-route-id="@customer.Id" class="btn btn-danger" title="Delete">
                                                <i class="fas fa-trash"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="7" class="text-center py-4">
                                    <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                                    <p class="text-muted">No customers found</p>
                                    @if (!string.IsNullOrEmpty(ViewBag.SearchTerm))
                                    {
                                        <a href="@Url.Action("Index")" class="btn btn-sm btn-outline-primary">Clear Search</a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <strong>Showing @Model.Items.Count of @Model.TotalItems customers</strong>
                </div>
                <div class="col-md-6">
                    @await Html.PartialAsync("_PaginationPartial", Model)
                </div>
            </div>
        </div>
    </div>
</div>
